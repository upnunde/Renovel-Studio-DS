{
  "$schema": "./README.md",
  "name": "PasswordInput",
  "slot": "password-input",
  "$description": "OS-중립 PasswordInput 정본. 출처: src/components/ui/password-input.tsx. Input + 표시/숨김 토글.",
  "role": "textbox",
  "$use": ["input"],
  "axes": { "size": { "values": ["sm", "default", "xl", "2xl"] } },
  "defaults": { "size": "default", "clearable": true },
  "styleSpec": {
    "control": { "$use": "input.styleSpec.control", "$note": "masked. endPadding: 토글만 pe-9, +클리어 pe-16" },
    "toggleButton": { "$note": "Button ghost, 우측. eye/eyeOff 아이콘 14px", "rest": { "fg": "{foreground-muted}" }, "hover": { "fg": "{foreground}" } },
    "clearButton": { "$use": "input-clear-button", "$note": "토글 왼쪽(right-9)에 배치" }
  },
  "states": ["rest", "focus", "disabled", "invalid"],
  "nativeNotes": {
    "masking": "웹 type password↔text 토글. iOS secureTextEntry, Compose PasswordVisualTransformation + 리빌 버튼.",
    "tabIndex": "인라인 액션 버튼은 Tab 순서 제외(field만). 네이티브도 동일 의도."
  }
}
